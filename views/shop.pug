extends layout/main-layout.pug

block stylesheets
  link(rel='stylesheet' href='/css/shop.css')

block content
  main#main-content(role="main")
    h1 Book Store
    if !props || props.length === 0
      h2 No book found!
    else
      h2 Featured Products
      .cards(aria-live="polite")
        each products in props
          .card
            img.card_img(
              src="/images/book.avif", 
              alt="Cover image of the book #{products.title}", 
              srcset="/images/book-small.avif 480w, /images/book-large.avif 800w", 
              sizes="(max-width: 600px) 480px, 800px"
            )
            .card_content 
              h3.card_title #{products.title}
              .card_des #{products.description || 'Discover this amazing book now!'}
              button(type="button", aria-label="Add #{products.title} to cart") Add to cart
  footer(role="contentinfo")
    p &copy; #{new Date().getFullYear()} Your Bookstore Name. All rights reserved.
